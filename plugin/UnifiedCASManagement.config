set (autostart true)
set (preconditions Platform)
set (callsign org.rdk.UnifiedCASManagement)

map()
    key(root)
    map()
        kv(mode ${PLUGIN_UNIFIEDCASMANAGEMENT_MODE})
        kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
    end()
end()
ans(configuration)
